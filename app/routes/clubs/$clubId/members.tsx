import { ActionFunction } from "remix";

export const action: ActionFunction = async ({ params, request }) => {
  return null;
};

export default function MembersPage() {
  return <div>club members page</div>;
}
